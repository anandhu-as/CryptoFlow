generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider  = "postgresql"
  url       = env("DATABASE_URL")
  directUrl = env("DIRECT_URL")
}

model Watchlist {
  id             String   @id @default(cuid())
  userId         String
  coinId         String
  coinName       String
  coinImage      String
  symbol         String
  addedPrice     Float    // price when user added the coin
  currentPrice   Float    
  priceChange    Float    @default(0) 
  addedAt        DateTime @default(now())
  updatedAt      DateTime @updatedAt

  @@unique([userId, coinId])
}